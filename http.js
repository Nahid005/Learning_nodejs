const http = require('http')
const fs = require('fs');
const { dirname } = require('path');

fs.writeFile()

let port = 5000;

// http.createServer( (req, res) => {
//     res.write(" Hello World Node js is beauty")
//     res.end();
// }).listen(port, () => {
//     console.log(` server is running ${port}`);
// })